* Use `uv run python ...` to run Python commands
* Place any temporary files, such as experiment scripts, output plots etc. into the `./tmp` folder
* You can use the existing tool `uv run python -m shallow_mlps.cli` to experiment with shallow MLPs and generate plots of their behavior
