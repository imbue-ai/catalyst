* Use `uv run python` for running any Python code.
* Run `uv run python -m unittest test_context_manager.py` after making changes to `context_manager.py`.
* Run `uvx ruff check` after changing any Python files.
* In the frontend, we don't use all-cap titles or labels. All titles and labels should follow Chicago style capitalization. (only exceptions: task/step state labels and the "AI SCIENTIST" title)
